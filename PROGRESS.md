# Сессия D1, часть 2 — первый сквозной прогон английских Помпей
Начало: 2026-09-13 22:35 · Лимит: 2 часа, до 00:35 · Статус: в работе

## Сейчас
MP4 собран и принят по кадрам. Разбираюсь, почему в рендере теряется ночной фон сцены «хук».

## План
- [x] окружение ноутбука: npm ci, python-пакеты, проверка ffmpeg/Chrome/GPU
- [x] `npm run doctor` — зелёный
- [x] `npm run build -- videos/pompeii-en` — прошёл целиком, автопроверка зелёная
- [x] контактный лист кадров из MP4 + «что видно»
- [x] пересборка после правок; попытка починить фон «хука» откачена — сделала сцены пустыми
- [ ] короткий опыт: рендер одного бита без шейдерного перехода ← сейчас
- [x] пороги автопроверки проверены на живом рендере: запас везде большой, кроме сравнения со снимком у «хука» (31,8 при 32)
- [ ] коммит, отчёт сессии

## Сделано
- 22:35 — прочитаны правила, план, ловушки, решения и отчёт прошлой сессии
- 23:14 — рабочее состояние восстановлено: автопроверка зелёная, контактный лист сверен
- 23:05 — попытка поднять клип сцены (`z-index: 25`) откачена: сцены 01 и 02 вышли пустыми, автопроверка упала
- 22:54 — ролик собран одной командой: 56,03 с, 455 МБ, автопроверка зелёная (формат, −14,1 LUFS / −2,0 dBTP, пустых сцен нет, 23 события меняют кадр, кадры совпадают со снимками)
- 22:49 — конвейер дошёл до рендера: голос 33,7 с, тайминги слов 84,9 с (whisper расслышал 119 из 120 слов), звук 20 с, index.html 1,4 с; lint 0 ошибок, check 0 ошибок; длительность ролика 56,03 с
- 22:44 — доставлены kokoro-onnx и chrome-headless-shell (в кэш, не в репозиторий); whisper.cpp собран, модель whisper 1,55 ГБ скачана
- 22:39 — `npm ci` (194 пакета, 2 мин) и `npm run doctor`: всё зелёное, кроме двух ожидаемых предупреждений про нескачанные модели
- 22:38 — ноутбук: Node v25.6.1, Python 3.10.12, ffmpeg 7.0.2, google-chrome есть, 158 ГБ свободно; доставлены scipy 1.15.3 и soundfile 0.14.0

## Проблемы
- На чистой машине не было `kokoro-onnx` и whisper.cpp — первый прогон упал на голосе. Kokoro поставлен через pip, whisper.cpp hyperframes собирает сам.
- Голос синтезируется пулом из 3 процессов, и каждый начал качать свои 330 МБ модели. Прогон остановлен, модель качается один раз; в `engine/src/voice.ts` первая строка теперь прогревает кэш. Строка в TRAPS.md.
- `npx @puppeteer/browsers install` кладёт chrome-headless-shell в корень репозитория — перенёс в `~/.cache/puppeteer`, hyperframes нашёл его сам и включил быстрый путь захвата.

## Дальше
Если сессия прервётся: `npm ci`, затем `npm run doctor` и `npm run build -- videos/pompeii-en`.

## Итог
Заполняется в конце.
