{
  "$schema": "../../scenes/schema.json#texture",
  "id": "grain",
  "name": "Плёночное зерно",
  "use": "Film grain over the whole frame, above the captions. Always on: the look sets its strength (look.grain), a video never lists it.",
  "layer": { "depth": "mid", "blend": "normal", "opacity": 1, "z": 40 },
  "params": {
    "dark": { "type": "number", "default": 0.16, "min": 0, "max": 1, "description": "Непрозрачность тёмных крупинок (стиль × look.grain)." },
    "light": { "type": "number", "default": 0.12, "min": 0, "max": 1, "description": "Непрозрачность светлых крупинок." },
    "fps": { "type": "number", "default": 24, "min": 1, "max": 60, "description": "Сколько раз в секунду зерно перескакивает." }
  },
  "seed": { "default": 0, "affects": null }
}
