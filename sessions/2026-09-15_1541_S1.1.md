# Сессия S1.1 — доводка панели, библиотека в library/, регрессия, терминал режиссёра
Начало: 2026-09-15 15:41 · Лимит: 3 часа (коммит к 18:41) · Статус: завершена

## Сейчас
Сессия завершена: итог ниже, отчёт — `sessions/2026-09-15_1541_S1.1.md`, один коммит «S1.1: доводка панели, библиотека в library/, регрессия, терминал», push не делался.

## План
- [x] чтение: ROADMAP, CLAUDE.md, DECISIONS, TRAPS, отчёт S1, studio/; прошлый PROGRESS (odyssey) → `sessions/2026-09-15_1412_odyssey_прервана.md`
- [x] Блок 0 — `npm run regress`, снимки до переноса (`--record`) 7/7; правило в CLAUDE.md, README, навыке, CONTRACT; слово убрано из репозитория; DECISIONS
- [x] Блок 1 — 11 разделов из engine/ в `library/`, engine/ — только код; регрессия после переноса 7/7
- [x] Блок 2 — вкладка «Режиссёр»: журнал, `director.log`, у Molasses и Odyssey — из транскриптов; `claude -p` из панели с allowlist, отказы в director.log
- [x] Блок 3 — терминал xterm.js + node-pty: живой claude, «/short для этого проекта», перечитывание битов после его правок, переживает переключение проектов и закрытие вкладки
- [x] Блок 4 — Look только отдельным экраном с превью на `sampleLine`; сайдбар с иконками Tabler; trim по умолчанию; `en.json` и язык в настройках
- [x] Блок 5 — отчёт, ROADMAP «Черновик от сессии», один коммит; финальный полный прогон регрессии остановлен на 2 из 7 — коммит по просьбе пользователя

## Сделано
- 15:41 — прочитан контекст; прошлый PROGRESS.md (сессия odyssey не закончена) перенесён в sessions/ с пометкой «прервана»
- 15:55 — `engine/src/regress.ts` и `npm run regress`; голос регрессии только из кэша (`HYGEN_VOICE_CACHE_ONLY`)
- 16:15 — `--record`: 7 снимков за 19 мин 50 с; листы пяти роликов совпали с листами в git 12/12 (Great Fire 14/14)
- 16:20 — панель: журнал и allowlist (`studio/director.ts`), терминал (`studio/terminal.ts`), вкладка «Режиссёр», слежение за файлами проекта, en.json (субагент), язык, сайдбар Tabler, Look убран из Библиотеки, защита API; node-pty 1.1.0 и @xterm/xterm 6.0.0 в зависимостях
- 16:25 — director.log Molasses и Odyssey из транскриптов Claude Code; проверка allowlist настоящим `claude -p`: 3 отказа записаны, 3 разрешения прошли (14 с, $0,21)
- 16:35 — перенос в `library/` (`git mv`, `LIBRARY_DIR`, пути в 21 файле кода и 108 текстах), `sampleLine`, trim по умолчанию, уникальность без несобранных черновиков; doctor, scenes, lint превью сцены, субтитров и карточки — зелёные; trim на копии хроники Titanic: 3–9 с → клип 6,0 с; панель пользователя перезапущена на новом коде
- 16:45 — регрессия после переноса: 6 из 7, у molasses плитка r2c4 3,10 при пороге 3,0 — зерно (до переноса та же плитка — 3,09)
- 16:45 — сквозная проверка терминала в headless Chrome: claude из «Открыть claude здесь» за 9 с правит `sees` бита, журнал и карточки битов перечитаны
- 16:48 — `compare_frames.py`: среднее по размытым 3×3 плиткам; `npm run regress -- --no-build` — 7 из 7
- 16:50 — передний план терминала через `/proc` (tpgid): «/short для этого проекта» печатает в claude, а не в bash; окружение claude очищено от меток сессии агента
- 16:53 — превью библиотеки: 83 пересчитано за 388 с, ошибок 0
- 17:01 — финальный полный прогон регрессии на итоговом коде: titanic 12/12, pompeii 12/12; остановлен на 2 из 7 — пользователь попросил коммит; листы всех семи роликов против снимков (`--no-build`) — 7 из 7, битых картинок нет
- 17:03 — отчёт, один коммит

## Проблемы
- odyssey-en: сценарий правили после озвучки — у 4 реплик нет дублей ElevenLabs (373 символа при остатке бюджета 59). Регрессия собирает голос только из кэша: эти реплики — Kokoro, символы не тратятся. Старый MP4 (14:26, сценарий до правок) отложен в `history/2026-09-15_1426-mp4/`.
- Помпеи: автопроверка была красной из-за уникальности против `pompeii-copy-en` — черновой копии из панели без сборки (в 16:09 в ней из панели удалён бит). Движок: несобранный черновик в уникальности не участвует.
- Панель пользователя (`npm run studio` с 14:06) после переноса упала бы на старых путях — перезапущена сессией в фоне (задач в ней не было), лог `.cache/studio/server.log`.
- Первая регрессия после переноса 6/7: шум зерна film-memory у molasses 04-wave. Снимки не перезаписывались: поправлено сравнение (TRAPS, DECISIONS).
- Терминал: диалог доверия claude по умолчанию «No, exit»; `pty.process` у node-pty называет шелл, даже когда работает claude; claude из панели, поднятой сессией агента, унаследовал её метки (`CLAUDE_CODE_MESSAGING_SOCKET`, child-session, effort) — всё починено, строки в TRAPS.

## Дальше
Приёмка S1.1 ревизором; push — пользователь. Odyssey — переозвучить 4 реплики после сброса бюджета ElevenLabs.

## Итог

**Блоки 0–5 сделаны.** Сессия 15:41 – 17:03 — 1 ч 22 мин из 3 ч. Субагент один (перевод en.json, 15:59–16:09); `claude -p` — одна проверка allowlist; интерактивный claude в терминале панели — три прогона сквозной проверки.

### «Готово, когда»
1. **Да.** Слова нет ни в одном файле репозитория (grep по всему дереву, кроме node_modules и .git): правило в CLAUDE.md заменено правилом 12 «Регрессия по всем роликам», README, навык, CONTRACT, MUSIC.md, look ember переписаны по смыслу, в архивах (DECISIONS, ROADMAP, roadmap/history, sessions) слово заменено механически. `npm run regress` работает и зелёный по всем семи не-proof роликам после переноса: сборка после переноса 6/7 (шум зерна у molasses) → сравнение по размытым плиткам → 7/7; финальный полный прогон на итоговом коде остановлен на 2 из 7 (titanic и pompeii 12/12) — коммит по просьбе пользователя; листы в коммите против снимков — 7/7.
2. **Да.** `library/`: looks, styles, textures, devices (с vendor/ и VENDOR.md), captions (пресеты и families.json), transitions, motion, scenes (HTML и JSON-рецепты, CONTRACT.md, schema.json), arcs, intents, assets (шрифты, карты, картина Волера, GSAP), music, previews, voices, regress. `engine/`: src, py, stage (рантайм), vendor (скрипты навыков), package.json, tsconfig.json, README.md.
3. **Да.** Вкладка «Режиссёр» у Molasses — один прогон, у Odyssey — два (восстановлены из транскриптов Claude Code), у Halifax и остальных — «сделан вручную». «Открыть claude здесь» запускает интерактивный claude с подпиской; claude поменял `sees` бита за 9 с, журнал и карточки битов перечитались без перезагрузки (`s11_director_after_claude_edit.png`, `s11_beats_after_claude_edit.png`).
4. **Да.** `claude -p` из панели — `--permission-mode dontAsk` с allowlist; проверка настоящим прогоном: запись в корень, `ls /` и чтение /etc/hostname отказаны и записаны событиями `denied` в director.log (`sessions/2026-09-15_1541_S1.1_allowlist-check.log`), чтение CLAUDE.md, запись в проект и `npm run scenes` прошли.
5. **Да.** Look — отдельный экран, превью разные (18 HOURS, 2:40 HOURS, 4,800 KM, Why is the sky blue?); сайдбар — белый текст выбранного пункта без плашки и полосы, иконки Tabler 20 px; trim из media.json становится in/out бита (хроника Titanic на копии проекта: клип 6,0 с вместо 11 с); `en.json` — 442 ключа, как в ru.json, язык в «Настройках».

### Блок 0 — регрессия
`npm run regress` пересобирает каждый не-proof проект со сборкой (черновик `pompeii-copy-en` пропускается) по два параллельно и сравнивает лист MP4 со снимком `library/regress/<id>.jpg`; `--record` пишет снимки, `--only`, `--jobs`, `--no-build`. Голос — только из кэша проекта. Расхождение кладёт рядом `.cache/regress/<id>.diff.jpg`.

| Ролик | Лист в git против `--record` | До переноса `--record` (15:55–16:15) | После переноса, сборка (16:25–16:45) | После правки сравнения `--no-build` | Финальный прогон |
|---|---|---|---|---|---|
| pompeii-en | 12/12 | записан · 5:59 · verify ✗ (уникальность против черновика-копии) | 12/12 · 6:03 · verify ✓ | 12/12 | 12/12 · 6:44 |
| titanic-en | 12/12 | записан · 5:35 | 12/12 · 5:36 | 12/12 | 12/12 · 6:12 |
| krakatoa-en | 12/12 | записан · 5:19 | 12/12 · 5:15 | 12/12 | остановлен до сборки |
| great-fire-en | 14/14 | записан · 5:19 | 14/14 · 5:14 | 14/14 | остановлен до сборки |
| halifax-en | 12/12 | записан · 5:10 | 12/12 · 5:21 | 12/12 | остановлен до сборки |
| molasses-flood-en | 11/12 (r2c4 3,09) | записан · 5:06 | 11/12 (r2c4 3,10 / блок 5,1) · 5:17 | 12/12 | остановлен до сборки |
| odyssey-en | листа не было | записан · 3:46 · ElevenLabs + Kokoro ×4 | 14/14 · 3:48 | 14/14 | остановлен до сборки |

Сравнение: среднее плитки теперь по размытым 3×3 плиткам, блок — по сырой разнице. Шум семи роликов после размытия ≤ 1,17 (до — до 3,10); сдвиг на 4 px даёт 14,0, оттенок +8 — 8,0, сдвинутую на 40 px подпись ловит блок (79,7 при пороге 40).

### Блок 1 — library/
- `git mv`: looks, textures, devices, transitions, arcs, intents, scenes и сверх списка промпта styles, assets, motion (иначе в engine/ остались бы данные); пресеты субтитров и `families.json` — в `library/captions/`; `engine/captions/word-by-word.html` (скин D1, не используется с D6) удалён.
- `LIBRARY_DIR` (util.ts, из `hygen.config.json → paths.library`); пути в 21 файле кода (движок и панель) и в 108 текстах (комментарии, сообщения, навык, README, CLAUDE.md, TRAPS, CONTRACT, VENDOR.md); файлы в `vendor/` не тронуты — их хэши в VENDOR.md.
- `publish/description.md` Помпей: путь кредита карты теперь `library/assets/maps/bay-of-naples.svg` — publish пишет путь файла.
- Проверено: typecheck движка и панели, `npm run doctor`, `npm run scenes`, lint превью year-odometer, субтитров karaoke и карточки вопроса, `build --no-render` копии Titanic, регрессия 7/7.
- Превью библиотеки: кэш по содержимому файлов, а пути в комментариях поменялись почти во всех элементах — `npm run library:previews` пересчитал 83 превью за 388 с по два параллельно, ошибок 0; из кэша — 4 look (пересчитаны раньше на `sampleLine`, 80 с) и 2 рецепта.

### Блок 2 — журнал режиссёра и allowlist
- `projects/<id>/director.log` — JSON-строки stream-json `claude -p` плюс события `hygen` (start с allowlist, denied, end). Вкладка «Режиссёр» показывает: прогоны (откуда, модель, длительность, ходы, токены вход/выход/кэш, стоимость, отказы), бриф, концепцию из research.md и арку, запросы `npm run media` (и из циклов `for q in …`) и скачанное, что выбрано и почему (research.md → Media), лицензии media.json, intents и «что видит зритель» по битам, грамматику, расход ElevenLabs по дублям, отказы allowlist, director.log сворачиваемо.
- Molasses: 1 прогон из транскрипта (14 мин, 17 ходов, выход 33k токенов, кэш 2,1M / 174k), 9 запросов, 5 скачиваний, ElevenLabs 605 символов. Odyssey: 2 прогона (`/short odyssey` 14 мин и параллельный `/short odyssey-en` 3 мин), 13 скачиваний, ElevenLabs 509.
- Allowlist `claude -p` из панели (флаги — README, раздел 8):

| Правило | Что пускает | Проверка |
|---|---|---|
| `Bash(npm run *)` | скрипты движка | `npm run scenes` — прошло; `ls /` — отказ |
| `Read(./**)`, `Glob`, `Grep` | чтение файлов репозитория | CLAUDE.md — прочитан; /etc/hostname — отказ |
| `Write(./projects/<id>/**)`, `Edit(./projects/<id>/**)` | запись только в свой проект | `projects/<id>/allowed.txt` — записан; `allowlist-denied.txt` в корне — отказ |
| `WebFetch(domain:en.wikipedia.org)` | исследование навыка (сверх списка промпта) | — |
| `TodoWrite` | список задач | — |
| остальное | `--permission-mode dontAsk`, `--setting-sources project`, `--strict-mcp-config` | отказ → событие `denied` в director.log |

### Блок 3 — терминал
- node-pty в сервере (один `bash -l` на панель, корень репозитория), xterm.js в странице, вывод — SSE с повтором буфера, ввод и размер — POST; только 127.0.0.1, запись — только с заголовком `X-Hygen`.
- Проверено в headless Chrome и через API: «Открыть claude здесь» (диалог доверия к папке подтверждён в тесте), claude правит project.json → журнал и карточки битов перечитаны; правка project.json снаружи на вкладке «Биты» → карточка перечитана с тостом; переключение на Halifax — тот же терминал, меняется подсказка «проект …»; закрытие браузера — shell жив; «Завершить» — shell остановлен; «/short для этого проекта» при запущенном claude печатает `/short <id>` в claude без Enter.

### Блок 4 — интерфейс
- Look: раздел Looks убран из Библиотеки, экран Look показывает `sampleLine` и превью на ней; поле `sampleLine` в форме нового look.
- Сайдбар: выбранный пункт — белый текст без плашки и полосы; иконки Tabler 3.34.1 (MIT) встроены в `studio/web/icons.ts`: layout-grid, plus, sparkles, palette, terminal, settings; пункт «Режиссёр» открывает вкладку последнего проекта.
- trim: `media.json → trim` — умолчание `stage.in/out`, поле за полем.
- `studio/i18n/en.json` (442 ключа), язык в «Настройках» (localStorage), недостающий ключ берётся из ru.json.

### Блок 5
- Krakatoa v2 (proof): лист S1 принят как текущий — изменения движка D6–D7 (DECISIONS).
- ROADMAP: строка S1.1 «черновик от сессии». Один коммит, push не делался.

### Скриншоты (`sessions/screens/`)
| Скриншот | Что видно |
|---|---|
| `s11_sidebar.png` | логотип hygen, иконки Tabler; выбранный «Look» — белым, без плашки и полосы |
| `s11_looks.png` | четыре мира с превью на своих репликах: 2:40 HOURS (abyss), Why is the sky blue? (bright-explainer), 18 HOURS (ember), 4,800 KM (storm); форма нового look с «репликой превью» |
| `s11_library_no_looks.png` | Библиотека без раздела Looks: текстуры … музыка, 87 элементов с превью |
| `s11_director_terminal_claude.png` | вкладка «Режиссёр»: живой claude в терминале панели |
| `s11_director_after_claude_edit.png` | в терминале — правка claude (`"sees"` у 01-hook), ниже журнал уже перечитан: в «Intents по битам» новый текст |
| `s11_beats_after_claude_edit.png` | вкладка «Биты»: у 01-hook «Ash falls on the forum…» — после правки claude |
| `s11_director_short_typed.png` | «/short для этого проекта» напечатал `/short zz-terminal-check` в поле claude без Enter |
| `s11_beats_reloaded.png` | правка project.json снаружи: карточка перечитана, тост «project.json изменён вне панели — карточки перечитаны» |
| `s11_director_journal_molasses.png` | журнал Molasses целиком: прогон, бриф, концепция и арка, 9 запросов и 5 скачиваний, что выбрано и почему, лицензии, intents, грамматика, ElevenLabs 605, director.log |
| `s11_director_journal_odyssey.png` | журнал Odyssey: два прогона, запросы и 13 скачиваний |
| `s11_director_halifax_manual.png` | Halifax: «сделан вручную», тот же терминал с подсказкой «проект halifax-en» |
| `s11_settings_en.png` | панель по-английски: Settings, Panel language — English |

В терминале на скриншотах журналов Molasses, Odyssey и Halifax — повтор буфера второй проверки, ещё до очистки окружения (строка «Transcript saving is off»).

### Команды
```bash
. ~/.nvm/nvm.sh
npm run regress                          # все не-proof ролики: сборка и сравнение листов (~20 мин)
npm run regress -- --record --only <id>  # перезаписать снимок после намеренного изменения (+ DECISIONS)
npm run regress -- --no-build            # сравнить готовые листы без пересборки
npm run studio                           # панель; вкладка проекта «Режиссёр» — журнал и терминал
npm run library:previews                 # превью, в том числе look на sampleLine
npx tsc -p engine/tsconfig.json && npx tsc -p studio/tsconfig.json
```

### Замеры
| Что | Факт |
|---|---|
| Регрессия, 7 роликов по 2 параллельно | 19 мин 50 с (запись), 20 мин 02 с (после переноса); финальный прогон остановлен на 2 из 7 (6 мин 12 с и 6 мин 44 с — параллельно с превью библиотеки) |
| Сборка ролика в регрессии | 5–6 мин в паре (соло 3–4 мин), odyssey 3 мин 46 с |
| Проверка allowlist `claude -p` | 14 с, $0,21 |
| Правка бита живым claude в терминале | 9 с от Enter до записи project.json |
| Превью look | 4 за 80 с |
| ElevenLabs | 0 символов (остаток 59 из 3 000) |

### Что не вышло или сделано иначе (честно)
- **WebFetch к Wikipedia — сверх allowlist промпта.** Навык исследует тему через `curl` к Wikipedia; из панели `curl` теперь отказан, режиссёр должен перейти на WebFetch сам. Полный `/short` из панели с allowlist в этой сессии не запускался — только проверочный прогон.
- **styles, assets и motion перенесены сверх списка**, `engine/captions/word-by-word.html` удалён — иначе engine/ не был бы «только код».
- **Первая регрессия после переноса — 6/7.** Снимки не перезаписывались: поправлено сравнение (зерно film-memory), пересчёт готовых листов 7/7. Финальный полный прогон на итоговом коде остановлен на 2 из 7 (titanic и pompeii 12/12) — пользователь попросил коммит; остальные пять собраны после переноса, но на правленом сравнении полной сборкой ещё не прогонялись.
- **Снимок odyssey-en — со смешанным голосом** (Kokoro ×4): его сценарий правлен после озвучки, а бюджет ElevenLabs — 59 символов. После сброса бюджета нужна переозвучка и `npm run regress -- --record --only odyssey-en` с записью в DECISIONS.
- **Сессия перезапустила панель пользователя** (была запущена со старым кодом, задач не было): теперь она работает в фоне (`.cache/studio/server.log`); чтобы вернуть её в свой терминал — остановить процесс `node studio/server.ts` и `npm run studio`.
- **Диалог доверия к папке** в интерактивном claude подтверждён сквозной проверкой; режим claude у пользователя — auto, правки не спрашивались.
- **Слово в архивах заменено механически** («образец», «контрольный»), смысл старых записей тот же; в восстановленном director.log Molasses — тоже.
- `pompeii-copy-en` — черновая копия пользователя с его правками из панели (снимок истории 16:09) — в коммите как есть.
- Подвал claude в xterm после смены размера иногда переносится узкой колонкой — косметика, не чинилось.
- Proof-проекты в регрессию не входят (по промпту).
