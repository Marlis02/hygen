# Сессия D3.5 — уникальность ролика (корректирующая)
Начало: 2026-09-14 10:13 · Лимит: 4 часа (оценка остатка в 13:43, коммит к 14:13) · Статус: завершена

## Сейчас
Сессия закрыта в 11:51: все четыре условия «готово, когда» выполнены, три ролика собраны на итоговом движке и проходят автопроверку с `uniqueness`. Всё в одном коммите, push не делался.

## План
- [x] Блок 0 — машина №2: doctor зелёный, образец Помпей/Titanic/Krakatoa собран на HEAD; план v4 (история); `.gitignore` для контактных листов; README «Машины»
- [x] Блок 1 — look-слой: `engine/looks/` (ember, abyss, storm), `look` в video.json, палитра → токены стиля, CSS-переменные `--hy-*`, `npm run scene -- <id> --look`
- [x] Блок 2 — библиотека текстур: grain, fire, embers, ash (из Помпей), rain, bubbles, smoke, fog (субагент), lightning, wind, snow
- [x] Блок 3 — медиафон бита: image/video, duotone/blur/ken-burns/parallax; фото «Титаника» дуотоном под счётчиком; видео — на тестовом ролике
- [x] Блок 4 — motion: камера, параллакс, 8 пресетов типографики, 6 пост-эффектов, 6 переходов модулями `engine/transitions/`; замер рендера
- [x] Блок 5 — концепция в навыке `/short`
- [x] Блок 6 — пересборка Titanic и Krakatoa, uniqueness в verify, `videos/_compare/contact-3.jpg`
- [x] Блок 7 — отчёт, строка в ROADMAP, один коммит

## Сделано
- 11:51 — отчёт перенесён в `sessions/2026-09-14_1013_D3.5.md`; один коммит «D3.5: уникальность ролика — look, текстуры, медиафон, motion, концепция», дерево чистое, push не делался
- 11:50 — Titanic пересобран с поправленным abyss: автопроверка и `uniqueness` зелёные, оттенок 194°, рендер 66,4 с, 72,3 МБ; кадры эффектов из финальных MP4 (вспышка и пепел Помпей, молнии, дымовые шторки и whip Krakatoa, рябь Titanic) совпадают со снимками; итоговая склейка `contact-3.jpg`
- 11:47 — `hyperframes check` у финального Titanic: 5 символов подписи на карте с контрастом 2,8–3,0:1 (туман abyss в screen осветлял низ кадра) — туман приглушён до 0,35 цветом ashLight, виньетка abyss 0,66/52; `check` — 0 замечаний по контрасту; Titanic пересобирается
- 11:46 — финальный Krakatoa: автопроверка и `uniqueness` зелёные, settled 23,1, рендер 94,1 с (параллельно с проверками Titanic)
- 11:43 — финальный Titanic: автопроверка и `uniqueness` зелёные, рендер 66,2 с
- 11:40 — финальные Помпеи на итоговом движке: автопроверка и `uniqueness` зелёные, рендер 98,9 с; контактный лист снова 12 из 12 с образцом
- 11:39 — worktree `.baseline` убран (контрольные листы и отчёты сохранены вне репозитория), `typecheck` и `doctor` зелёные
- 11:38 — строка «Черновик от сессии D3.5» в ROADMAP; DECISIONS (11 решений), TRAPS (+4 ловушки), CONTRACT.md (разделы «Look ролика», «Текстуры», «Фон бита», «Motion», «Переходы»), README
- 11:36 — финальные полные сборки Помпей, Titanic, Krakatoa на итоговом движке (в фоне)
- 11:35 — переходы вынесены модулями в `engine/transitions/<id>/transition.js`: кадры стыка 03→04 Помпей — пиксель в пиксель с образцом, settle-кадры 6 из 6; снимки переходов Titanic и Krakatoa не изменились; глубина сцены из `scene.json → depth`
- 11:33 — перепроверка всех трёх: `uniqueness` зелёная у всех пар и по кадрам (29° / 194° / 72°, ближайшая пара 44°)
- 11:29 — Krakatoa (storm) собран: автопроверка зелёная, 44,1 с, 91 МБ, рендер 87,0 с; склейки `videos/_compare/before-3.jpg` и `contact-3.jpg`
- 11:28 — видеофон проверен на сгенерированном ролике (зациклен до длины бита); чётные размеры и запись кэша через `.part` (TRAPS)
- 11:26 — Titanic (abyss) собран: автопроверка и `uniqueness` зелёные, 35,1 с, 72,5 МБ, рендер 63,6 с; раздел Concept в research.md обоих роликов; навык `/short` переписан
- 11:24 — Titanic → abyss, Krakatoa → storm правками только в video.json; снимки до рендера: у каждого свой мир
- 11:21 — `verify`: оттенок акцента по settle-кадрам и проверка `uniqueness` (`engine/src/uniqueness.ts`)
- 11:18 — полная сборка Помпей через ember: автопроверка зелёная, контактный лист MP4 совпал с образцом в 12 плитках из 12, рендер 97,5 с против 95,5 с
- 11:16 — bloom без backdrop-filter; типографика перехватывает opacity и clip-path вместо kill твинов (TRAPS)
- 11:10 — превью motion: map-marker под storm, year-odometer под abyss с фото фоном
- 11:08 — runtime motion `engine/motion/runtime.js`, флаг превью `--beat`, текстовые слоты в 7 `scene.json`; регрессия Помпей 6 из 6
- 11:00 — субагент сдал rain, bubbles, smoke, fog; мои lightning, wind, snow; `engine/py/background.py`
- 10:45 — регрессия Помпей без рендера: settle-кадры пиксель в пиксель
- 10:44 — fire, embers, ash — из частиц сцен Помпей
- 10:40 — палитры ember, abyss, storm на превью counter-title
- 10:36 — `look.ts`, `textures.ts`, `layers.ts`, реестры motion и переходов; зерно — текстура
- 10:37 — образец на HEAD (worktree `.baseline`) собран: Помпеи 5 мин 48 с, Titanic 4 мин 15 с, Krakatoa 4 мин 53 с — все зелёные
- 10:22 — план v4 → ROADMAP.md; `.gitignore` для контактных листов
- 10:17 — `npm run doctor` зелёный с первого раза
- 10:13 — прочитаны план, правила, ловушки, решения, контракт, отчёт D2+D3, навык, код движка и все сцены

## Проблемы
- `ROADMAP.new.md` в корне не было — план v4 пришёл текстом в промпте, положен в ROADMAP.md оттуда.
- Контрольных контактных листов прошлой машины в git нет — образец пересобран на HEAD в отдельном worktree до первой правки движка.
- `npm`/`node` не видны в неинтерактивном шелле (Node через nvm) — README «Машины».
- Сцены вставляют цвета в строки canvas и SVG-атрибуты, где `var()` не работает: look подменяет токены при сборке сцены, CSS-переменные `--hy-*` — только для слоёв движка (DECISIONS).
- В `ember` зерно сначала стояло и в `textures` (рисовалось бы дважды) — убрано, теперь это ошибка сборки.
- bloom на `backdrop-filter` в MP4 затягивал кадр мутью; kill твинов сцены оставлял `opacity: 0` (год не появлялся); ffmpeg падал на нечётной ширине фона и оставлял битый кэш — всё переделано, три строки в TRAPS.
- Размытый дуотон литографии за «1883» давал кислотные пятна — фон Krakatoa без blur и слабее.
- `hyperframes check` у Titanic: туман abyss опускал контраст подписи карты до 2,8:1 — look abyss поправлен (TRAPS). Остаётся одна ошибка `check`, не блокирующая: «M» у отсчёта глубины (у правого края кадра) под виньеткой — в MP4 «3,800 M» читается.

## Дальше
D4 по плану: слепой `/short "Great Fire of London 1666"` (проверит и шаг концепции: `uniqueness` не пропустит копию Помпей), звук по событиям (гром молний, переходы), корректировки и долги D2+D3 — подробности в «Итог → Дальше».

## Итог

**Все четыре условия «готово, когда» выполнены.**
1. **Да.** `npm run doctor` на компьютере №2 зелёный с первого раза. Помпеи через look `ember`: контактный лист итогового MP4 совпал с образцом HEAD в 12 плитках из 12 (среднее отличие 1,0–1,6 из 255 — шум кодирования); settle-кадры и кадры стыка 03→04 совпали со снимками образца пиксель в пиксель.
2. **Да.** Titanic пересобран на `abyss`, Krakatoa — на `storm`. Правки только в `video.json` (и раздел Concept в `research.md`), HTML сцен не тронут. В каждом ролике параллакс под камерой, кинетическая типографика и пост-эффекты. Три контактных листа рядом (`videos/_compare/contact-3.jpg`) читаются как три разных ролика; «до» — `videos/_compare/before-3.jpg`.
3. **Да.** `uniqueness` в `verify` зелёная у всех трёх. Оттенок акцента по look — 16° / 185° / 68°, по settle-кадрам — 29° / 194° / 72°; ближайшая пара Помпеи–Krakatoa отличается на 43° по кадрам; наборы текстур и последовательности сцен у всех разные.
4. **Да, но не вслепую.** В навыке `/short` появился шаг «Визуальная концепция» до раскадровки и шаблон нового look объектом в `video.json`. Сам навык в этой сессии не прогонялся: концепции Titanic и Krakatoa я написал по его шаблону. Слепой тест — D4, блок 1.

### По блокам
Блоки шли внахлёст: образец и сборки рендерились в фоне, пока писался код.

**Блок 0 — машина №2 (10:13–10:37).** doctor зелёный без починок: Node v25.6.1 через nvm, HyperFrames 0.8.36, ffmpeg 6.1.1, Python-пакеты, модели Kokoro и whisper уже в кэше. Отличие машины — `npm`/`node` не видны в неинтерактивном шелле (README «Машины»). Контактных листов прошлой машины в git не было, поэтому образец пересобран на HEAD в отдельном worktree `.baseline` до первой правки движка: Помпеи 5 мин 48 с (голос 23 с, whisper 78 с, рендер 95,5 с, мастеринг 92 с), Titanic 4 мин 15 с, Krakatoa 4 мин 53 с, автопроверка зелёная у всех. План v4 → ROADMAP.md, v3 → `roadmap/history/2026-09-14_0146.md`; `.gitignore`: `**/renders/*` и `!videos/*/renders/*.contact.jpg`.

**Блок 1 — look-слой (10:30–10:40).** `engine/src/look.ts`: загрузка по id или объектом с `extends`, неизвестное поле — ошибка, палитра → токены стиля (новый акцент выводит deep/hot/light, `groundTint` перекрашивает нейтральную шкалу со светлотой как была; цвет, равный цвету стиля, не пересчитывается — ember даёт токены байт в байт), CSS-переменные `--hy-*` для слоёв движка. Три look с тремя строками описания: `ember` = Помпеи, `abyss` (#46D2E0 на #0A141A, secondary янтарь #F2C46D), `storm` (#CFE05A на #0E130F, secondary #9FB4FF). `npm run scene -- <id> --look abyss` или `--look '{json}'`.

**Блок 2 — текстуры (10:36–11:00).** Контракт `texture.json` + `texture.html` (`engine/src/textures.ts`, `layers.ts`): подкомпозиция над сценой и под субтитрами, параметры `density`, `speed`, `angle`, `color` (токен палитры), `opacity`, `seed`, `blend`, `depth`, тип `cues` для времён на словах и ударах. 11 текстур: grain (вынесено из `assemble.ts` — Помпеи пиксель в пиксель), fire (crater/horizon/glow), embers, ash (flakes/pumice) — из частиц сцен Помпей; rain, bubbles, smoke, fog — субагент; lightning (событие для грома), wind, snow — я. Включение из look (весь ролик) или бита (поверх).

**Блок 3 — медиафон (10:50–11:30).** `background` у любого бита: `image` или `video`, `treatment` ken-burns / parallax / blur / duotone (списком), `focus`, `opacity`, `depth`, `blend`, `zoom`; лицензия обязательна. Обработка при сборке (`engine/py/background.py`, кэш), дуотон в палитру бита, слой в режиме lighten над тёмной землёй сцены. Titanic — фото корабля дуотоном под «2:40 HOURS», Krakatoa — литография под «1883». Видео — ffmpeg зацикливает до длины бита, без звука.

**Блок 4 — motion и переходы (10:55–11:36).** `engine/motion/runtime.js` без WebGL: камера (none, push-in, pull-out, pan, tilt, handheld по шуму от сида, shake на ударах, амплитуда из look), параллакс bg/mid/scene/fg (глубина сцены из `scene.json → depth`), 8 пресетов типографики на слоты из `scene.json → text` (look — по виду текста, бит — по слоту), 6 пост-эффектов с силой 0–1. Переходы — модули `engine/transitions/<id>/transition.js`: hard-cut, flash и ash-burst (вынесены байт в байт), whip, water-ripple, smoke-wipe; look задаёт переход по умолчанию и на удар, бит переопределяет. Замер рендера (образец → сборки после правок): Помпеи 95,5 → 97,5 и 98,9 с (+2–4 %), Titanic 60,4 → 63,6–66,4 с (+5–10 %), Krakatoa 75,7 → 87,0 с (+15 %; финальная сборка — 94,1 с, но шла одновременно с проверками Titanic) — до порога 1,5× далеко.

**Блок 5 — навык (11:20–11:27).** `.claude/commands/short.md`: подготовка смотрит looks и занятые другими роликами палитры и текстуры; шаг «Визуальная концепция» (настроение в 2 фразах, ключевой цвет темы, look — выбрать или создать объектом с обоснованием, 2–3 картинки, текстуры по битам, камера, типографика, пост-эффект, переходы) пишется в `research.md → ## Concept`; раскадровка — 5–8 битов в любом порядке, повтор сцены в другом режиме, карта не обязательна, минимум один медиафон и два пресета типографики; в таблице автопроверки — что делать при упавшей `uniqueness`.

**Блок 6 — пересборка и уникальность (11:20–11:48).** `verify` считает оттенок акцента по settle-кадрам (круговое среднее hue с весом chroma², `palette` в отчёте) и сравнивает ролик со всеми в `videos/`: меньшая из разниц (по look и по кадрам) ≥ 30° или разный набор текстур; одинаковая последовательность сцен — предупреждение. Склейки `contact-3.jpg` (после) и `before-3.jpg` (до) — `engine/py/contact_montage.py`.

### Три ролика
| | Помпеи | Titanic | Krakatoa |
|---|---|---|---|
| look | ember · #FF5A1F (16°, по кадрам 29°) | abyss · #46D2E0 (185°, по кадрам 194°) | storm · #CFE05A (68°, по кадрам 72°) |
| текстуры | зерно | зерно, туман, пузыри; на глубине — плотные пузыри и взвесь | зерно, дым, пепел; молнии на «loudest» и «blast»; дождь и ветер на волне |
| медиафон | — | фото RMS Titanic дуотоном под «2:40 HOURS» | литография 1888 дуотоном под «1883» |
| камера | нет (образец) | push-in 0,6; pan на карте, tilt на глубине, pull-out на фото | handheld 0,9, тряска 0,8; на волне 1,3 и 1,2; финал push-in |
| типографика | нет | count-roll, mask-wipe, typewriter (подписи и год), slide, glow-pulse (финал) | stagger (числа), split-reveal (заголовки, TSUNAMI), slide (подписи), count-roll (отсчёт) |
| пост-эффекты | нет | bloom 0,4, vignette-pulse, blur-pull на глубине, light-leak на 1985 | flicker 0,35, chromatic на ударах (на хуке 0,6) |
| переходы | flash + ash-burst на 03→04 | water-ripple на ударе 02→03 | smoke-wipe на ударе 02→03, whip в картину, smoke-wipe в финал |
| длина, размер | 56,0 с, 115,0 МБ | 35,1 с, 72,3 МБ | 44,1 с, 91,0 МБ |
| рендер: образец → D3.5 | 95,5 → 97,5–98,9 с | 60,4 → 63,6–66,4 с | 75,7 → 87,0 с (94,1 с — параллельно с проверками) |
| автопроверка | зелёная | зелёная | зелёная (settled 23,1 при пороге 32) |

### Что видно
- **До** (`before-3.jpg`): три ролика — пепельно-серые кадры одной раскладки; у Помпей и Krakatoa один оранжевый герой, у Titanic голубой на той же пепельной земле; те же зерно, виньетка и вспышка с пеплом.
- **После** (`contact-3.jpg`): Помпеи — как были, огонь и пепел. Titanic — бирюзово-синий подводный мир: «2:40» светится поверх фото корабля, всплывают пузыри, в глубину стык расходится кольцами ряби, дробь 1/3 с янтарной чертой. Krakatoa — серо-зелёная мгла с дымом и лаймовыми акцентами: литография за «1883», дымовая шторка над картой, косой дождь над «TSUNAMI», whip в литографию.

### Честно о том, что не так или не проверено
- «Look переопределяет токены через CSS-переменные» — наполовину: сцены получают палитру таблицей токенов при сборке (canvas и SVG-атрибуты сцен не читают `var()`), переменные `--hy-*` на корне читают только слои движка.
- fire, embers, ash — копии частиц сцен Помпей с параметрами; из самих сцен не удалены, иначе поменялся бы образец. Из новой библиотеки Помпеи берут зерно и переходы.
- Видеофон проверен на сгенерированном тестовом ролике (ffmpeg testsrc2): зацикливание, дуотон, кадры в снимках. Настоящего видео из Commons в роликах нет.
- Хук Krakatoa: число загорается только на «four» (≈ 3,5 с) — сценарий D3; правило в навыке уже есть, реплика не переписывалась, чтобы остаться в правках «только video.json».
- `settled` у Krakatoa 23,1 при пороге 32 — выше, чем у других (дым, flicker, тряска); пока проходит.
- Навык не проверен вслепую; гром на событиях молнии и звуки переходов не сделаны (D4–D5); шум `hyperframes check` не трогал (D4, блок 2).

### Время и ресурсы
- Сессия 10:13–11:51: 1 ч 38 мин из 4 ч.
- Субагент: 1, только в блоке 2 (rain, bubbles, smoke, fog) — 18,5 мин, ≈ 165 тыс. токенов. Одновременно работало не больше 2 агентов.
- Сборки: образец трёх роликов ≈ 15 мин в фоне; полные сборки с кэшем голоса — Помпеи 3 мин 46 с, Titanic 2 мин 38 с, Krakatoa 3 мин 18 с; сборка без рендера ≈ 4 с.
- Коммит один, push не делался.

### Срезано
Ничего — ни из обязательного, ни по правилам срезания D3.5: все пост-эффекты, снег и молния, видеофон, шесть переходов и восемь пресетов типографики сделаны.

### Команды
```bash
. ~/.nvm/nvm.sh                                       # компьютер №2: если npm не виден
npm run doctor
npm run build -- videos/titanic-en                    # look abyss
npm run build -- videos/krakatoa-en                   # look storm
npm run verify -- videos/pompeii-en                   # автопроверка с palette и uniqueness
npm run scenes                                        # сцены, параметры, якоря, текстовые слоты
npm run scene -- counter-title --look abyss
npm run scene -- map-marker --look storm --beat '{"type":{"count":"stagger"},"camera":{"preset":"handheld","shake":1},"post":[{"id":"chromatic","strength":0.8}]}'
npm run scene -- fraction-finale --textures '[{"id":"rain"}]'
python3 engine/py/compare_frames.py <образец>.contact.jpg <новый>.contact.jpg
python3 engine/py/contact_montage.py videos/_compare/contact-3.jpg <лист>:"подпись" …
```
В Claude Code: `/short "тема"` — теперь с концепцией до раскадровки.

### Дальше (D4)
1. Слепой `/short "Great Fire of London 1666"`: пожар ложится на `ember`, но `uniqueness` не пропустит копию Помпей (тот же акцент, тот же набор текстур) — режиссёру придётся сдвинуть акцент или взять свои текстуры. Хорошая проверка шага концепции.
2. Звук: гром на событиях молнии (`build/layers.json → textures[].events`), удары и свисты переходов — по `look.sound`.
3. Хук Krakatoa (число позже 2 с), шум `hyperframes check`, долги D2+D3 по плану.
