# Сессия D1, часть 1 — каркас движка и английский конвейер
Начало: 2026-09-13 17:21 · Лимит: до ~18:10 (пользователь закончил день) · Статус: остановлена по лимиту

## Сейчас
Сессия остановлена. Код движка написан, проверка типов проходит, но одной командой ролик ещё не собирался.

## План
- [x] git: кэши вон из индекса, коммит CLAUDE.md и sessions/; GitHub не создан — gh не авторизован
- [x] engine/: CLI на TypeScript (`build`, `doctor`, `verify`), HyperFrames 0.8.36 закреплён, шрифты и GSAP в проекте, копии скриптов навыков
- [x] перенос конвейера Помпей в код: сцены, субтитры «слово за словом», звук, приглушение, мастеринг — **не прогнан**
- [x] videos/pompeii-en/video.json: 6 битов на английском, Kokoro, whisper — **не прогнан**
- [x] автопроверка MP4 написана — **пороги не откалиброваны на рендере**
- [ ] первый прогон `npm run build -- videos/pompeii-en`, исправления, контактный лист ← следующий шаг
- [ ] README.md
- [ ] проверка на копии videos/pompeii-ru (из исправленного задания части 1)

## Сделано
- 17:21 — прочитаны правила, план, ловушки, решения, память
- 17:36 — git: `__pycache__` и бэкапы Studio вынесены из индекса, CLAUDE.md и sessions/ закоммичены
- 17:41 — зависимости закреплены: hyperframes, @hyperframes/core, @hyperframes/shader-transitions 0.8.36, gsap 3.14.2 (npm workspaces)
- 17:44 — Kokoro и whisper на английском проверены: произношение верное, тайминги точные, кроме коротких слов в паузах
- 17:50 — 6 сцен перенесены в engine/scenes/pompeii: без правок Studio, подписи на английском, GSAP из проекта, крючки варпа времени
- 18:00 — модули конвейера, автопроверка, doctor, video.json; tsc и py_compile проходят

## Проблемы
- Перепутано задание: делалась полная версия D1 (английский ролик) вместо части 1 на час (сборка копии pompeii-ru на русском). Код английской части написан; прогон и pompeii-ru — на следующий заход.
- В исходниках примера остались правки Studio после финального рендера (карта 71°, пустой кадр 02 в рендере «картина»). Перенесено состояние финального рендера; строка в TRAPS.md.
- gh не авторизован — приватный репозиторий hygen на GitHub не создан.
- Картина Волера без записи о лицензии — в английскую версию не взята (DECISIONS.md).

## Дальше
1. `npm run doctor`, затем `npm run build -- videos/pompeii-en`. Где вероятнее всего сломается: варп таймлайнов (GSAP getChildren/startTime), локальный GSAP в lint, carve, пороги автопроверки.
2. Контактный лист из MP4 и «что видно»; откалибровать пороги engine/py/verify_mp4.py.
3. Если нужна часть 1 по исправленному заданию: копия примера в videos/pompeii-ru и сборка на русском.
4. README.md. После `gh auth login` — приватный репозиторий hygen и push.

## Итог
Работает: CLI загружается (`node engine/src/cli.ts help`), TypeScript проходит `tsc`, Python-скрипты компилируются. Сквозной прогон не делался, поэтому конвейер пока не проверен: он считается рабочим, только когда `build` выдаст MP4 и тот пройдёт автопроверку.

Команды (после `npm ci` в корне):
- `npm run doctor` — проверка окружения
- `npm run build -- videos/pompeii-en` — голос → тайминги слов → звук → сцены → index.html → lint/check → рендер → мастеринг → автопроверка
- `npm run verify -- videos/pompeii-en` — автопроверка готового MP4 и контактный лист

Заняло: ~45 минут (17:21–18:05).
Срезано: сквозной прогон, контактный лист и «что видно», README, pompeii-ru, GitHub.
