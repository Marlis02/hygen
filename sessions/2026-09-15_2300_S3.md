# Сессия S3 — Редактор, панель на Preact + Vite, монохром
Начало: 2026-09-15 23:28 · Лимит: 5 часов · Статус: завершена

## Сейчас
Сессия завершена: один коммит «S3: панель на Preact + Vite, монохром, создание из брифа, редактор — дорожки, скраббинг, перетаскивание», push не делался.

## План
- [x] ROADMAP.new → ROADMAP.md, старый в `roadmap/history/2026-09-15_2300.md`
- [x] окружение: doctor, studio:check
- [x] блок 1 — карта ролика `build/timeline.json` + API (агент); доводка: ref слов, флаг «изменено»
- [x] блок 0 — Preact + Vite: каркас, сервер, роутинг, i18n, живой канал
- [x] блок 0.6 — монохром (проход скриншотами — в конце)
- [x] блок 0.5 — создание из брифа, бейдж диалога с «Завершить»
- [x] блок 0 — все экраны на Preact, `check.ts` через preact-render-to-string, `dom-shim` удалён
- [x] блок 2 — редактор: дорожки под плеером
- [x] блок 3 — скраббинг по композиции (агент): `renderSeek`, кадр за 25–60 мс
- [x] блок 4 — перетаскивание, Ctrl+Z
- [x] блок 5 — долги (агент): журнал экранным текстом, `machine` в снимках, `behind-subject` двумя строками, `/short` v3.5
- [x] замеры, скриншоты, README/CLAUDE/TRAPS/DECISIONS, отчёт, коммит

## Сделано
- 23:28 — план заменён; `ROADMAP.new.md` в корне не лежал — план пришёл вложением, записан из него
- 23:36 — `doctor` зелёный, `studio:check` 5 из 5 (сборка Помпей 265 с) — база до переделки
- 23:39 — preact 10.29.8, @preact/signals 2.11.2, vite 8.3.0, preact-render-to-string 6.7.0; Vite — middleware сервера на 5177, `studio:build` → `studio/dist`, `studio:dist`
- 23:46 — все экраны на Preact (библиотека, look, настройки, ассеты — субагент); монохром; бриф создаёт проект-скелет со статусом brief и бюджетом; бейдж диалога в сайдбаре с «Завершить»; `loadSpec` пускает пустые биты, сборка отвечает «нет битов — это бриф»
- 23:48 — 14 экранов в headless Chrome: ошибок консоли нет, ни одного не-GET запроса при обходе
- 23:50 — блок 1 готов: сборка пишет `build/timing.json` и `build/timeline.json`, `GET /api/projects/:id/timeline` в трёх режимах (сборка / перепроекция / оценка), `voice.charsPerSecond` Kokoro 14.4, ElevenLabs 16.8; пауза после реплики — `pad[1]`, музыка — `music.in`
- 23:55 — `studio:check --no-build` 6 из 6 (новая строка «создание из брифа» сначала поймала `sound.drone обязателен` у скелета — исправлено); старые `web/*.ts` и `dom-shim.ts` удалены
- 23:59 — редактор Помпей в headless Chrome: 7 дорожек, клик → seek 30,0 с, кадр-подсказка из листа, форма бита справа; граница 01-hook → `pad [0.3, 1.2]`, метки «оценка» и «изменено, пересоберите»; Ctrl+Z вернул project.json байт в байт
- 00:03 — блок 3 (агент): композиция в iframe на паузе, `__player.renderSeek`, 30 seek — среднее 27 мс, максимум 52; встроено в редактор переключателем «MP4 / Композиция», протяжка по шкале — скраббинг
- 00:04 — блок 5 (агент): `ref`/`refEnd` у слов, флаг «изменено» в карточке, `machine` в `snapshot.json`, журнал диалога экранным текстом (синтетика 86 → 9 строк), `behind-subject` двумя строками, `/short` v3.5; `library:previews` — 14 пересчитано, 77 из кэша, 0 ошибок
- 00:06 — README, CLAUDE.md (правило 17), DECISIONS, TRAPS; `test2-en` удалён (к этому моменту без `brief.json` и `project.json`, 12 МБ остатков, диалогов нет)
- 00:09 — скриншоты `sessions/screens/s3_*`: до/после, бриф, редактор, композиция, перетаскивание с меткой «изменено»
- 00:13 — `studio:check` 6 из 6 со сборкой Помпей (235 с, карта ролика записана сборкой); «музыка не пишется после устройства» — не баг: в сценарии дорожка ушла за край окна 1000 px, плеер редактора уменьшен до 240 px, всё влезает
- 00:16 — `studio:build` → `studio/dist` за 0,4 с; замер правок DOM за 35 с сборки: «Проекты» 0, проект 0 вне лога задачи

## Проблемы
- `projects/test2-en` в начале сессии уже был не «только brief.json»: осиротевший диалог довёл его до `project.json` и сборки, а во время чтения папку удаляли снаружи (в репозитории работает панель пользователя на 5177 и живой диалог `great-fire-en`). Чужой диалог не трогаю, панель проверяю на своих портах.

## Дальше
Приёмка S3 ревизором; push — пользователь. Перезапустить панель (`npm run studio`): панель на 5177 работает на старом коде сервера. Открыть «Редактор» у Помпей и подвигать границу, устройство (у Помпей их нет — Великий пожар), музыку.

## Итог

Все блоки S3 сделаны, срезанного нет. Субагентов было три, одновременно не больше двух: карта ролика и долги движка (блоки 1 и 5), порт четырёх экранов, скраббинг по композиции. Время: 23:28 → 00:30, около часа из пяти.

| Блок | Сделано | Срезано |
|---|---|---|
| 0 Preact + Vite | 14 экранов на Preact + signals (TSX, `studio/web/src`); Vite — middleware того же сервера на 5177; `studio:build` → `studio/dist`, `studio:dist`; `check.ts` через Vite SSR + preact-render-to-string, `dom-shim` удалён | — |
| 0.5 Создание | бриф пишет скелет проекта (статус `brief`, бюджет из обязательного поля, без битов), ничего не запускает; бейдж диалогов в сайдбаре и список в «Режиссёре» с «Завершить»; проверка в `studio:check` | — |
| 0.6 Монохром | токены белый/серый/графит, цвет только у ошибок и предупреждений, без теней и свечений | — |
| 1 Карта ролика | `build/timing.json` + `build/timeline.json` из сборки, `GET /api/projects/:id/timeline`: сборка / перепроекция / оценка; `ref` слов; `voice.charsPerSecond` | — |
| 2 Редактор | плеер, 7 дорожек, курсор на rAF, клик — seek, Ctrl+колесо, кадр-подсказка из листа, форма бита справа | — |
| 3 Скраббинг | композиция в iframe на паузе, `__player.renderSeek`, переключатель «MP4 / Композиция», протяжка по шкале | — |
| 4 Перетаскивание | граница бита → `pad[1]`, устройство → `at` со снапом к слову своего бита, музыка → `music.in` по долям; Ctrl+Z / Ctrl+Shift+Z (50); метка «изменено, пересоберите» | — |
| 5 Долги | журнал диалога экранным текстом, `machine` в `snapshot.json`, `behind-subject` двумя строками, `/short` v3.5 | — |

**Замеры.**
- Скраббинг по композиции: 30 seek — среднее 27 мс, медиана 24, максимум 52; в редакторе с rAF — 43–61 мс.
- Сборка Помпей кнопкой: 265 с в начале сессии, 235 с в финальном `studio:check`.
- `studio:check` — 6 из 6; `library:previews` — 14 пересчитано, 77 из кэша, 0 ошибок.
- 14 экранов в headless Chrome — ни одной ошибки консоли.
- Две пересборки Помпей не изменили ни одного отслеживаемого файла.
- Правки DOM за 35 с сборки: «Проекты» — 0, экран проекта — 0 вне лога задачи (+1 дописанная строка лога); S2.1 было 2. Первый замер показал 7, но в окно попала моя же временная копия проекта — перемерено на чистой сборке

**Команды.** `npm run studio` (сервер + Vite одной командой), `npm run studio:build` и `npm run studio:dist`, `npm run studio:check [-- --no-build]`, `node engine/src/cli.ts timeline <проект> [--read]`.

**Что не вышло или не проверено.**
- **Бейдж идущего диалога** проверен кодом и API. Скриншота нет: на тестовой панели диалог не запускали, чтобы не звать claude ради картинки.
- **Журнал диалога** очищен только на синтетике и старом журнале great-fire (1712 → 450 строк). Старые журналы сырого вывода не хранят, настоящая проверка — на следующем живом диалоге.
- **Ожидание видеокадра в композиции** написано, но не проверено: собранных проектов с `<video>` нет.
- **`behind-subject` в две строки:** превью пересчитаны, глазами не смотрели.
- **Панель пользователя на 5177** работает на старом коде сервера, а старых файлов страницы больше нет. После сессии её нужно перезапустить (`npm run studio`); живые диалоги при этом завершатся.

**Долги.**
- Odyssey — «Финал на ElevenLabs», когда будет ключ.
- `claude` в PATH ноутбука — битая ссылка, чинить руками.
- Снимки `history/` на каждый коммит движка — наблюдаем.
- Content ID Titanic.
