# Ночная сессия D2+D3 — библиотека сцен, Short «Titanic», ИИ-режиссёр
Начало: 2026-09-14 00:46 · Лимит: 7 часов (стоп работы в 06:46, до 07:46 — отчёт) · Статус: завершена

## Сейчас
Сессия закрыта в 01:46: библиотека из 7 сцен, Помпеи и Titanic из неё, навык `/short` и Krakatoa по нему — все три ролика проходят автопроверку. Всё закоммичено, push не делался.

## План
- [x] Блок 0 — план v2: история, CLAUDE.md, DECISIONS.md, коммит
- [x] Блок 1 — D2-A: контракт и schema.json, токены стиля, 7 сцен с параметрами, регрессия Помпей, битрейт, вспышка вместо шейдера
- [x] Блок 2 — D2-B: Short «Titanic» — сценарий, силуэт Атлантики, фото, video.json, сборка
- [x] Блок 3 — D3: навык `/short`, source на цифрах в автопроверке, прогон «Krakatoa 1883»
- [x] Блок 4 — отчёты: sessions/, строка в ROADMAP «Прогресс», финальный коммит (без push)

## Сделано
- 01:46 — отчёт сессии, строка «Черновик от сессии» в ROADMAP, README (команды `scene`, `scenes`, навык `/short`)
- 01:44 — коммит D3; навык поправлен по итогам прогона (цифра хука раньше, `scale-gauge` без сценографии не брать)
- 01:43 — автопроверка: `settled` сравнивает кадр со снимком выше полосы субтитров; Krakatoa, Помпеи и Titanic перепроверены — все зелёные
- 01:41 — Krakatoa собран: 44,1 с, 90,9 МБ, источники у всех цифр; автопроверка упала на `settled` 34,2 (субтитр в снимке)
- 01:39 — Krakatoa: `npm run build -- videos/krakatoa-en --no-render` — 2 мин, голос 22 с, тайминги слов 62 с (whisper расслышал 98 из 101 слова), предупреждений по якорям и длительностям нет; `hyperframes check`: 4 container_overflow от наезда камер (как у Помпей и Titanic), текст «под» прозрачным canvas вспышки — шум, не блокирует
- 01:37 — навык `/short`: исследование Krakatoa (10 цифр с цитатами, `videos/krakatoa-en/research.md`), силуэт Зондского пролива (Natural Earth, с лицензией; без записи лицензии сборка не пустила — проверка работает), литография 1888 (Royal Society, public domain), `video.json` на 6 битов
- 01:34 — **Помпеи пересобраны с короткой вспышкой: контактный лист совпал с D1 в 11 плитках из 12** (среднее отличие 1,1–1,6 из 255; «хук» отличается ночным фоном — это починка ловушки шейдера). Автопроверка зелёная, 115 МБ, рендер 89 с
- 01:30 — **Titanic собран из библиотеки без правок HTML сцен, автопроверка зелёная**: 35,1 с, 72,5 МБ (20,7 МБ на 10 с), −14,0 LUFS / −2,0 dBTP, 12 событий, источники у всех цифр; рендер 56 с. `hyperframes check` отметил контраст второй строки кредита в picture-zoom 3,96:1 — поднял прозрачность подписи (дефект сцены, не дыра контракта)
- 01:30 — первая регрессия Помпей из библиотеки: автопроверка зелёная, 56,03 с, 115 МБ (20,5 МБ на 10 с), −14,1 LUFS / −2,0 dBTP, 24 события меняют кадр, источники у всех цифр; рендер 88 с в 4 потока (было 195 с в 1 поток), сборка из кэша 3 мин 37 с. Контактный лист совпал с D1 в 10 плитках из 12 (среднее отличие 1–1,5 из 255): «хук» теперь на ночном фоне (починка ловушки шейдера), settle-кадр 03 засвечен разгоном вспышки — правлю. Навык `.claude/commands/short.md` написан
- 01:26 — силуэт Северной Атлантики из Natural Earth 50m (`engine/assets/maps/north-atlantic.svg` + лицензия, генератор `engine/py/trace_map.py`); `videos/titanic-en/video.json` — 6 битов, у всех цифр источники (Wikipedia, Commons), правки только в video.json
- 01:24 — субагент сдал `map-marker`, `year-odometer`, `fraction-finale` (12 мин): lint 0, дефолты = Помпеи; в контракт добавлены условия с `&&` и ранняя проверка битов до голоса; `videos/pompeii-en/video.json` переписан на параметры и якоря сцен, у всех цифр источники (Wikipedia); битрейт: x264 crf 18 / slow / maxrate 16M / bufsize 32M / faststart в шаге мастеринга, автопроверка `size` ≤ 25 МБ на 10 с и `sources`; переход 03→04 — CSS-вспышка с всплеском пепла (без WebGL)
- 01:16 — сцены `counter-title`, `scale-gauge` (+ direction down, scenery ocean), `picture-zoom` (из вставки-картины; картина Волера теперь с лицензией из Wikimedia Commons), `pyroclastic-flow` (геройская) — lint 0, превью на дефолтах совпадает с кадрами Помпей, параметры Titanic и `--tone cold` работают; фото «Титаника» скачано с записью лицензии
- 01:05 — контракт: `engine/scenes/CONTRACT.md`, `schema.json`, проверка в коде `engine/src/contract.ts`; стиль на токенах `style.json` (цвета с холодным, шрифты, размеры, субтитры, зерно, виньетка, безопасная зона); команда `npm run scene -- <id>` — превью сцены без голоса
- 00:48 — блок 0: план v2 закоммичен (`roadmap/history/2026-09-13_1700.md`, CLAUDE.md, DECISIONS.md)
- 00:46 — прочитаны ROADMAP.new.md, CLAUDE.md, TRAPS.md, DECISIONS.md, отчёт D1 часть 2, код движка и сцены Помпей

## Проблемы
- На экране Помпей «+100 YEARS»: гипс Фиорелли — 1863 год, это +115 лет. Для регрессии оставлено как было, исправить текстом и параметром в следующей сессии.
- Трассировка силуэта из Natural Earth вернула пустую карту: Дуглас–Пекер на замкнутом кольце (первая точка = последней) выкидывал всё. Починено в `engine/py/trace_map.py`.
- Titanic: `hyperframes check` — контраст второй строки кредита в `picture-zoom` 3,96:1; поднята непрозрачность подписи в сцене (дефект сцены, не дыра контракта; HTML ради данных Titanic не правился). Titanic собран до этой правки.
- Krakatoa: первая автопроверка упала на `settled` из-за субтитра в снимке — починено в `engine/py/verify_mp4.py`, строка в TRAPS.md.
- `hyperframes check` у всех трёх роликов пишет «ошибки» `container_overflow` (наезд камер выходит за клип) и «текст под canvas вспышки» — это шум, кадры MP4 в порядке; `check` не блокирует сборку.
- Мелкие ошибки кода по ходу (конфликт имени параметра в `warpKnots`, откат `\s*` в регулярке проверки шрифтов) — пойманы проверкой типов и первым превью.

## Дальше
1. **D4, первым делом:** `/short` в свежей сессии на новую тему — проверить навык без контекста этой ночи и замерить расход токенов на Short (гипотеза ROADMAP: ≤ 15 % пятичасового окна).
2. **Помпеи «+100 YEARS» неточно:** гипс Фиорелли — 1863 год, это +115 лет. Нужны реплика («More than a century later…») и `delta: 115` — решение ревизора, потому что меняется эталонный кадр регрессии.
3. **Дыры контракта** (нашёл субагент): вершина на карте стоит жёстко (нужен `peakAt`), в `year-odometer` при `section: false` жёсткая раскладка и лишний откат камеры, `fraction-finale` только однозначные числа, событие `scale fills` при числителе = знаменателю.
4. **Кадр волны Krakatoa пустой:** сценографии цунами у `scale-gauge` нет — кандидат в D5 (текстуры и новые сцены).
5. Шум `hyperframes check`: пометить наезд камер `data-layout-allow-overflow`, canvas вспышки — `data-layout-allow-occlusion`.
6. Titanic 35 с — короче целевых 45–60 с навыка; Krakatoa 44 с.
7. Внутри токена-числа («1748.») слова получают неровные тайминги (у «eight» 0,05 с) — посмотреть `engine/src/words.ts`.

## Итог

Из трёх условий «готово, когда»: (1) **да** — Помпеи из библиотеки, 115 МБ (< 150), контактный лист совпал с D1 в 11 плитках из 12, отличие — ночной фон «хука» (починка); (2) **да** — Titanic без правок HTML сцен, автопроверка зелёная; (3) **да, с оговоркой** — Krakatoa по навыку дал MP4 с источниками у всех цифр без ручных правок, но первая автопроверка упала на дефекте самой проверки (субтитры в снимке); починен движок, тот же MP4 прошёл.

### D2-A — контракт, токены, библиотека сцен

**Сделано**
- Контракт сцены: `engine/scenes/CONTRACT.md`, `engine/scenes/schema.json`, проверка тем же кодом в `engine/src/contract.ts`. Сцена — папка `engine/scenes/<id>/` с `scene.html` и `scene.json`: плоские параметры (тип, дефолт = Помпеи, описание, `figure`, условие `if` с `&&`), именованные якоря вместо чисел-опорных времён, `duration {min,max}`, `settle`, события для автопроверки, `seed`, `safeZone`. Бит в `video.json`: `params`, `anchors`, `tone` (accent/cold), `seed`, `sources`. Сборка падает на лишнем параметре, неверном типе, неизвестном якоре, слове якоря не из реплики, картинке или силуэте без записи лицензии, литеральном цвете или шрифте в шаблоне.
- Стиль «Документальный тёмный» на токенах: `engine/styles/documentary-dark/style.json` — цвета (с холодным и тонами hero/heroDeep/heroHot/heroLight), шрифты, размеры, стиль субтитров, зерно, виньетка, безопасная зона. Субтитры, виньетка и зерно тоже берутся из токенов.
- 7 сцен: `counter-title`, `map-marker`, `scale-gauge` (+ режим «глубина» с океаном и тонущим кораблём), `year-odometer`, `picture-zoom` (из вставки-картины; картина Волера вернулась с лицензией из Commons), `fraction-finale`, геройская `pyroclastic-flow` (обрушение и лавина Помпей не ложатся на общие сцены). Три сцены перевёл субагент за 12 минут, четыре — я.
- Превью сцены без голоса: `npm run scene -- <id> [--params json] [--tone cold] [--seed n]` — lint + снимки + контактный лист за ~7 с. `npm run scenes` — список сцен, параметров и якорей.
- Регрессия: `videos/pompeii-en/video.json` переписан на параметры и якоря, все цифры с источниками. Контактный лист совпал с D1 в 11 плитках из 12 (среднее отличие 1,1–1,6 из 255); «хук» отличается ночным фоном — это починка ловушки шейдера.
- Битрейт: libx264 crf 18 / slow / maxrate 16M / bufsize 32M / faststart в шаге мастеринга (у `hyperframes render` нет preset/maxrate). Автопроверка `size` падает при > 25 МБ на 10 с, `sources` — без ссылки у цифры.
- 1.6 сделано: переход 03→04 — CSS-вспышка костяного цвета со всплеском пепла вместо шейдера `flash-through-white`.

**Цифры**
- Помпеи: 56,03 с, **115 МБ** (было 455), 20,5 МБ на 10 с, 16,4 Мбит/с, −14,1 LUFS / −2,0 dBTP, 24 события меняют кадр, settled 10,4.
- Рендер: **89 с в 4 потока** (D1: 195 с в 1 поток). Мастеринг с кодированием 77 с (было 6 с — это цена x264 slow). Сборка из кэша 3 мин 23 с.

**Команды**: `npm run build -- videos/pompeii-en` · `npm run scene -- counter-title` · `npm run scenes`.

**Срезано**: ничего из обязательного.

### D2-B — Short «Titanic»

**Сделано**
- `videos/titanic-en/video.json`: 6 битов по раскадровке — «2:40 HOURS» (минуты в формате clock) → карта Северной Атлантики с меткой и «≈600 KM OFF NEWFOUNDLAND» → глубина «3,800 M» (корабль опускается на дно) → «1985 +73 YEARS» → фото RMS Titanic полосой, снег, подпись → «1/3 SURVIVED». Тон cold, у каждой цифры источник (`research.md`).
- Силуэт Северной Атлантики — из Natural Earth 50m инструментом `engine/py/trace_map.py` (с записью лицензии). Фото — F. G. O. Stuart, 1912, public domain, Wikimedia Commons (с записью лицензии).
- HTML сцен ради Titanic не правился. Но честно: параметры для Titanic (формат clock, режим глубины, `fit: band`, tone) закладывались в контракт заранее, когда я его проектировал, — это не слепая проверка контракта.

**Цифры**: 35,1 с, **72,5 МБ** (20,7 МБ на 10 с), −14,0 LUFS / −2,0 dBTP, 12 событий, автопроверка зелёная; рендер 56 с, сборка с нуля ≈ 6 мин.

**Команда**: `npm run build -- videos/titanic-en`.

**Срезано**: ничего. Не по плану: ролик короче 45 с (35 с).

### D3 — ИИ-режиссёр

**Сделано**
- Навык `.claude/commands/short.md` — `/short "тема"` в Claude Code, без API: исследование через Wikipedia API с дословными цитатами в `videos/<id>/research.md` → сценарий 5–7 битов → выбор сцены по таблице и параметры по `scene.json` → `video.json` с источниками → `npm run build -- videos/<id> --no-render` → `npm run build`. В навыке шаблоны исследования, `video.json`, звука, трассировки карты, поиска картинки на Commons и таблица «что упало в автопроверке → что делать».
- Проверка фактов в движке: `sources` у каждой цифры на экране (параметры-цифры, строки и списки с цифрами, цифры реплики → `sources.text`). `verify` падает без ссылки, `build` предупреждает ещё до голоса.
- Прогон «Krakatoa 1883» по навыку, режиссёр — я в этой сессии: 10 цифр с цитатами (4 800 км, ≥ 36 417 погибших, волны > 30 м, 165 деревень и городов, 27.08.1883, волна давления обошла Землю, две трети острова); силуэт Зондского пролива трассировкой Natural Earth (сборка сначала не пустила силуэт без записи лицензии — проверка сработала); литография 1888 из отчёта Royal Society (public domain); 6 битов; проверка до рендера — без предупреждений; полная сборка → MP4.
- **Первая автопроверка Krakatoa упала**: `settled` 34,2 при пороге 32 в сцене волны. Расхождение только в полосе субтитров: снимок композиции ещё держал «destroyed.», а в MP4 группа уже погасла. Починен движок — сравнение со снимком идёт выше y 1420 (TRAPS, DECISIONS). Тот же MP4 прошёл автопроверку без пересборки; Помпеи и Titanic перепроверены — зелёные.
- **Ручных правок `video.json` после навыка не было.** Правки навыка по итогам прогона — 2: цифра хука должна звучать в первой половине реплики (у Krakatoa число загорается только на 4-й секунде, до этого на экране искра); `scale-gauge` без сценографии не брать — кадр волны вышел почти пустым (шкала в углу и слово TSUNAMI). Ролик с этими правилами не пересобирался, чтобы не править `video.json` после навыка.

**Цифры**: Krakatoa 44,1 с, **90,9 МБ** (20,6 МБ на 10 с), −14,0 LUFS / −2,0 dBTP, 13 событий меняют кадр, источники у всех цифр. От темы до MP4 ≈ 10 мин (01:31–01:41): исследование и раскадровка ≈ 5 мин, сборка до рендера 2 мин (голос 22 с, тайминги слов 62 с), полная сборка 2 мин 43 с (рендер 71 с, мастеринг 58 с). Плюс 3 мин на починку автопроверки.

**Команды**: в Claude Code `/short "Krakatoa 1883"`; руками — `npm run build -- videos/krakatoa-en`, `npm run verify -- videos/krakatoa-en`.

**Срезано**: из обязательного — ничего. Не сделано: независимая проверка навыка в свежей сессии — прогон вёл тот же агент, который писал навык и сцены, так что навык проверен не вслепую.

### Что видно (контактные листы `videos/*/renders/*.contact.jpg`)
- **Помпеи**: горящие «18 HOURS» на ночном фоне → карта залива с POMPEII и ≈ 11,000 RESIDENTS → столб пепла до 30 KM и PUMICE → огненная лавина ≈ 100 → 1748 +100 YEARS над разрезом с гипсом → 1/3 STILL BURIED.
- **Titanic**: голубые «2:40 HOURS» остывают в серый → Ньюфаундленд и метка TITANIC, ≈ 600 KM OFF NEWFOUNDLAND → корабль на дне под отсчётом 3,800 M → 1985 +73 YEARS → фото RMS Titanic полосой со снегом → 1/3 SURVIVED.
- **Krakatoa**: «4,800 KM» гаснет в серый → 1883 из-под пепла с огненной линией → Зондский пролив, KRAKATOA, 36,000 LIVES LOST, JAVA · SUMATRA → почти пустой кадр «30 M / TSUNAMI» → литография извержения → 2/3 GONE.

### Время и ресурсы
- Сессия 00:46–01:46: **1 ч 00 мин** из лимита 7 ч; блоки 1–3 уложились в первые 60 мин.
- Субагент: 1, только на шаге 1.3 (3 сцены, 12 мин, ≈ 170 тыс. токенов). Одновременно работало не больше 2 агентов.
- Коммиты: план v2 `eb5faab`, D2-A `08ce880`, D2-B `ecc5b81`, D3 `c887d7b`, отчёт — финальный. **Push не делался** (по заданию сессии; в ROADMAP в строке про ночную сессию написано «коммит и push» — пушит пользователь).
- ElevenLabs пропущен: ключа нет.

